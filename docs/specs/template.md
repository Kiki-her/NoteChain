# [機能名] 機能仕様書

**バージョン:** 1.0  
**対象 Feature:** `NoteChain/Features/[FeatureName]/`  
**担当 ViewModel:** `[FeatureName]ViewModel`  
**ステータス:** Draft / Review / Approved

---

## 概要

（1-2文で機能の目的を説明してください）

---

## ユーザーフロー

```
ステップ 1
  ↓
ステップ 2
  ↓
ステップ 3（条件分岐がある場合は記述）
  ├─ 成功: ...
  └─ 失敗: ...
```

---

## 画面・コンポーネント一覧

| ファイル | 役割 |
|---------|------|
| `[FeatureName]View.swift` | メイン画面 |
| `[Component]View.swift` | 再利用可能コンポーネント |

---

## ViewModel API

### 公開プロパティ

| プロパティ | 型 | 初期値 | 説明 |
|-----------|-----|--------|------|
| `isLoading` | `Bool` | `false` | ローディング状態 |
| `errorMessage` | `String?` | `nil` | エラーメッセージ |

### 公開メソッド

| メソッド | シグネチャ | 説明 |
|---------|-----------|------|
| `load()` | `() async` | データを読み込む |

---

## データモデル

**使用する @Model:** `Note`

| プロパティ | 型 | 用途 |
|-----------|-----|------|
| `id` | `UUID` | 識別子 |
| `transcript` | `String` | 表示テキスト |

---

## エラーケース

| AppError ケース | 発生タイミング | ユーザー向けメッセージ |
|---------------|--------------|---------------------|
| `.swiftDataSaveFailed` | 保存時 | 保存に失敗しました |

---

## 受け入れ基準（Given-When-Then）

```
Given: [前提条件]
When:  [操作]
Then:  [期待結果]
```

```
Given: [前提条件]
When:  [操作]
Then:  [期待結果]
```

---

## 依存モジュール

| 依存先 | 用途 |
|--------|------|
| `Shared/Models/Note` | SwiftData エンティティ |
| `Shared/Navigation/Router` | 画面遷移 |

---

## 非機能要件

| 指標 | 目標値 |
|------|--------|
| 処理時間 | < Xms |
| メモリ | < XMB |
