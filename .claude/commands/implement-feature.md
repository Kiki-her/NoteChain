# /implement-feature [feature]

指定された機能を実装します。`.claude/skills/implement-feature/SKILL.md` のワークフローに従います。

## 入力

`[feature]`: recording / keywords / notes-list / settings / subscription / onboarding

## 実行

`.claude/skills/implement-feature/SKILL.md` のステップ 1-6 を順番に実行する。

各ステップの完了後、次のステップに進む前に確認を取る。
